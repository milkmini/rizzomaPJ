extends ../layout/main

block variables
    - var title = 'about';
    - var description = 'xxx';
    - var pageClass = 'about-page';

block content
    section
        .container
            +item('item title', 'item descr')
    example-component


block script
    script(type='text/javascript').
        const foo = true;
        let bar = function() {};
        if (foo) {
        bar(1 + 5)
        }